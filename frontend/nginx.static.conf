server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # Fichiers buildés par Vite : noms hashés (immuables), cache long.
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    # index.html ne doit jamais être mis en cache : c'est lui qui référence les assets hashés du build courant.
    location / {
        add_header Cache-Control "no-cache";
        try_files $uri /index.html;
    }
}
